"""Vendored data assets bundled with cleopatra.

Currently holds ``magics_presets.json`` -- colour data and parameter labels
derived from the ECMWF Magics style library (Apache-2.0); see
``MAGICS_NOTICE.txt``. Kept as a package so the assets are importable via
``importlib.resources`` and shipped in the wheel.
"""
