"""Glyphs for values sampled over a 2-D domain (a grid or mesh).

Deliberately re-exports nothing, matching the package root: import from the
submodule, e.g. `from cleopatra.glyphs.gridded.array_glyph import ArrayGlyph`.

Submodules: `array_glyph` (scalar field on a regular grid / raster),
`mesh_glyph` (scalar field on an unstructured UGRID mesh), `vector_glyph`
(vector field drawn as arrows, barbs, or streamlines).
"""
