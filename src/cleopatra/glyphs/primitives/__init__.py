"""Glyphs for explicit geometry you pass in (points, lines, polygons, paths).

Deliberately re-exports nothing, matching the package root: import from the
submodule, e.g. `from cleopatra.glyphs.primitives.scatter_glyph import
ScatterGlyph`.

Submodules: `scatter_glyph` (points), `line_glyph` (lines / bars / bands),
`polygon_glyph` (filled polygons / choropleth), `flow_glyph` (colour- and
width-encoded polylines, Sankey / flow style).
"""
