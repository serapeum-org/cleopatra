"""Glyphs that summarise the distribution of a dataset.

Deliberately re-exports nothing, matching the package root: import from the
submodule, e.g. `from cleopatra.glyphs.stats.histogram_glyph import
HistogramGlyph`.

Submodules: `histogram_glyph` (1-D / 2-D histograms), `kde_glyph` (2-D
kernel-density estimate).
"""
